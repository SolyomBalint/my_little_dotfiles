{ ... }:

{
  # System-level configuration that is specific to this user. Machine-wide
  # programs and services live in hosts/<host>/, user-facing packages in
  # ./home.nix, and group membership in flake.nix's mkUser.
}
